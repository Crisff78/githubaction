name: Basic workflow

on:
  push:
    branches:
      - basico-workflow

jobs:
  do-things:
    name: Do Things
    runs-on: ubuntu-latest

    steps:
      - name: Logrado
        run: echo "Logrado", echo "Estoy Feliz"
      
